<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters="clr-namespace:CPvC.UI.Converters"
        xmlns:System="clr-namespace:System;assembly=mscorlib" x:Class="CPvC.UI.Forms.MainWindow"
        mc:Ignorable="d"
        Title="CPvC" Height="705.125" Width="784.535" Closing="Window_Closing" Loaded="Window_Loaded" KeyDown="Window_KeyDown" KeyUp="Window_KeyUp" SizeToContent="WidthAndHeight" ResizeMode="NoResize" Icon="../Resources/cpvc.png">
    <Window.Resources>
        <converters:Ticks x:Key="ticksConverter" />
        <converters:IsNotNull x:Key="isNotNullConverter" />
        <converters:RunningIcon x:Key="runningIconConverter" />
        <converters:Invisibility x:Key="invisibilityConverter" />
        <converters:BooleanInverter x:Key="booleanInverterConverter" />
        <Style x:Key="MachineMenuItemStyle" TargetType="{x:Type MenuItem}">
            <Setter Property="IsEnabled" Value="{Binding ActiveMachine, Converter={StaticResource isNotNullConverter}, Mode=OneWay}" />
        </Style>
        <Style x:Key="MachineButtonStyle" TargetType="{x:Type Button}">
            <Style.Resources>
                <Style TargetType="{x:Type Image}">
                    <Style.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Style.Resources>
            <Setter Property="IsEnabled" Value="{Binding ActiveMachine, Converter={StaticResource isNotNullConverter}, Mode=OneWay}" />
        </Style>
        <CollectionViewSource x:Key="Machines" Source="{Binding ViewModel.Machines}"/>
        <CollectionViewSource x:Key="OpenMachines" Source="{Binding ViewModel.Machines}" Filter="OpenMachines_Filter" IsLiveFilteringRequested="True">
            <CollectionViewSource.LiveFilteringProperties>
                <System:String>RequiresOpen</System:String>
            </CollectionViewSource.LiveFilteringProperties>
        </CollectionViewSource>
    </Window.Resources>
    <Grid AllowDrop="True" Margin="0,0,0,0" Background="#FFF0F0F0">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <Menu Height="30" Margin="0" VerticalAlignment="Top" VerticalContentAlignment="Center">
            <MenuItem Header="File">
                <MenuItem x:Name="_newMenuItem" Header="New..." Click="NewMenuItem_Click"/>
                <MenuItem x:Name="_openMenuItem" Header="Open..." Click="OpenMenuItem_Click"/>
                <MenuItem x:Name="_closeMenuItem" Header="Close" Click="CloseMenuItem_Click" Style="{StaticResource MachineMenuItemStyle}"/>
                <Separator/>
                <MenuItem x:Name="_exitMenuItem" Header="Exit" Click="ExitMenuItem_Click"/>
            </MenuItem>
            <MenuItem x:Name="_cpvcMenuItem" Header="CPvC" Style="{StaticResource MachineMenuItemStyle}">
                <MenuItem Header="Drive A:">
                    <MenuItem.Icon>
                        <Image Width="16" Height="16" Source="../Resources/floppy.png" />
                    </MenuItem.Icon>
                    <MenuItem x:Name="_driveAMenuItem" Header="Load..." Click="DriveAMenuItem_Click"/>
                    <MenuItem x:Name="_driveAEjectMenuItem" Header="Eject" Click="DriveAEjectMenuItem_Click"/>
                </MenuItem>
                <MenuItem Header="Drive B:">
                    <MenuItem.Icon>
                        <Image Width="16" Height="16" Source="../Resources/floppy.png" />
                    </MenuItem.Icon>
                    <MenuItem x:Name="_driveBMenuItem" Header="Load..." Click="DriveBMenuItem_Click"/>
                    <MenuItem x:Name="_driveBEjectMenuItem" Header="Eject" Click="DriveBEjectMenuItem_Click"/>
                </MenuItem>
                <MenuItem Header="Tape">
                    <MenuItem.Icon>
                        <Image Width="16" Height="16" Source="../Resources/tape16.png" />
                    </MenuItem.Icon>
                    <MenuItem x:Name="_tapeMenuItem" Header="Load..." Click="TapeMenuItem_Click"/>
                    <MenuItem x:Name="_tapeEjectMenuItem" Header="Eject" Click="TapeEjectMenuItem_Click"/>
                </MenuItem>
                <Separator/>
                <MenuItem x:Name="_pauseMenuItem" Header="Pause" Click="PauseMenuItem_Click">
                    <MenuItem.Icon>
                        <Image Width="16" Height="16" Source="../Resources/pause16.png" />
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem x:Name="_resumeMenuItem" Header="Resume" Click="ResumeMenuItem_Click">
                    <MenuItem.Icon>
                        <Image Width="16" Height="16" Source="../Resources/resume16.png" />
                    </MenuItem.Icon>
                </MenuItem>
                <Separator/>
                <MenuItem x:Name="_resetMenuItem" Header="Reset" Click="ResetMenuItem_Click">
                    <MenuItem.Icon>
                        <Image Width="16" Height="16" Source="../Resources/reset16.png" />
                    </MenuItem.Icon>
                </MenuItem>
                <Separator/>
                <MenuItem x:Name="_addBookmarkMenuItem" Header="Add Bookmark" Click="AddBookmarkMenuItem_Click">
                    <MenuItem.Icon>
                        <Image Width="16" Height="16" Source="../Resources/bookmarkadd16.png" />
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem x:Name="_previousBookmarkMenuItem" Header="Back to Previous Bookmark" Click="PreviousBookmarkMenuItem_Click">
                    <MenuItem.Icon>
                        <Image Width="16" Height="16" Source="../Resources/bookmarkprev16.png" />
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem x:Name="_jumpToBookmarkMenuItem" Header="Bookmarks..." Click="JumpToBookmarkMenuItem_Click">
                    <MenuItem.Icon>
                        <Image Width="16" Height="16" Source="../Resources/bookmark16.png" />
                    </MenuItem.Icon>
                </MenuItem>
                <Separator/>
                <MenuItem x:Name="_renameFileMenuItem" Header="Rename machine..." Click="RenameFileMenuItem_Click"/>
                <MenuItem x:Name="_compactFileMenuItem" Header="Compact machine file" Click="CompactFileMenuItem_Click"/>
            </MenuItem>
        </Menu>
        <ToolBarTray x:Name="toolBarTray" Margin="0" VerticalAlignment="Top" Grid.Row="1" Background="#FFF0F0F0">
            <ToolBar x:Name="toolBar" ToolBarTray.IsLocked="True" HorizontalContentAlignment="Left" VerticalAlignment="Center" VerticalContentAlignment="Center" Background="#FFF0F0F0">
                <Button x:Name="_driveAButton" Height="25" VerticalAlignment="Top" Width="40" Click="DriveAButton_Click" Style="{StaticResource MachineButtonStyle}">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="../Resources/floppy.png" Width="16" Height="16" Stretch="None" RenderOptions.BitmapScalingMode="NearestNeighbor" RenderOptions.EdgeMode="Aliased"/>
                        <TextBlock><Run Text="A:"/></TextBlock>
                    </StackPanel>
                </Button>
                <Button x:Name="_driveBButton" Height="25" VerticalAlignment="Top" Width="40" Click="DriveBButton_Click" Style="{StaticResource MachineButtonStyle}">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="../Resources/floppy.png" Width="16" Height="16" Stretch="None" RenderOptions.BitmapScalingMode="NearestNeighbor" RenderOptions.EdgeMode="Aliased"/>
                        <TextBlock><Run Text="B:"/></TextBlock>
                    </StackPanel>
                </Button>
                <Button x:Name="_tapeButton" Height="25" VerticalAlignment="Top" Width="25" Click="TapeButton_Click" Style="{StaticResource MachineButtonStyle}">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="../Resources/tape16.png" Width="16" Height="16" Stretch="None" RenderOptions.BitmapScalingMode="NearestNeighbor" RenderOptions.EdgeMode="Aliased"/>
                    </StackPanel>
                </Button>
                <Separator/>
                <Button x:Name="_pauseButton" Height="25" VerticalAlignment="Top" Width="25" Click="PauseButton_Click" Style="{StaticResource MachineButtonStyle}">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="../Resources/pause16.png" Width="16" Height="16" Stretch="None" RenderOptions.BitmapScalingMode="NearestNeighbor" RenderOptions.EdgeMode="Aliased"/>
                    </StackPanel>
                </Button>
                <Button x:Name="_resumeButton" Height="25" VerticalAlignment="Top" Width="25" Click="ResumeButton_Click" Style="{StaticResource MachineButtonStyle}">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="../Resources/resume16.png" Width="16" Height="16" Stretch="None" RenderOptions.BitmapScalingMode="NearestNeighbor" RenderOptions.EdgeMode="Aliased"/>
                    </StackPanel>
                </Button>
                <Button x:Name="_resetButton" Height="25" VerticalAlignment="Top" Width="25" Click="ResetButton_Click" Style="{StaticResource MachineButtonStyle}">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="../Resources/reset16.png" Width="16" Height="16" Stretch="None" RenderOptions.BitmapScalingMode="NearestNeighbor" RenderOptions.EdgeMode="Aliased"/>
                    </StackPanel>
                </Button>
                <Separator/>
                <Button x:Name="_addBookmarkButton" Height="25" VerticalAlignment="Top" Width="25" Click="AddBookmarkButton_Click" Style="{StaticResource MachineButtonStyle}">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="../Resources/bookmarkadd16.png" Width="16" Height="16" Stretch="None" RenderOptions.BitmapScalingMode="NearestNeighbor" RenderOptions.EdgeMode="Aliased"/>
                    </StackPanel>
                </Button>
                <Button x:Name="_seekToLastBookmarkButton" Height="25" VerticalAlignment="Top" Width="25" Click="SeekToLastBookmarkButton_Click" Style="{StaticResource MachineButtonStyle}">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="../Resources/bookmarkprev16.png" Width="16" Height="16" Stretch="None" RenderOptions.BitmapScalingMode="NearestNeighbor" RenderOptions.EdgeMode="Aliased"/>
                    </StackPanel>
                </Button>
            </ToolBar>
        </ToolBarTray>
        <TabControl x:Name="_machineTabControl" Margin="5,5,5,5" Grid.Row="2" Height="576" Width="768" SelectedItem="{Binding ActiveItem, Mode=TwoWay}">
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <Image Width="16" Height="16" Stretch="None" Source="{Binding Core.Running, Converter={StaticResource runningIconConverter}, Mode=OneWay}"/>
                        <TextBlock Text="{Binding Name, Mode=OneWay}"/>
                    </StackPanel>
                </DataTemplate>
            </TabControl.ItemTemplate>
            <TabControl.ContentTemplate>
                <DataTemplate>
                    <Grid MouseLeftButtonUp="ScreenGrid_MouseLeftButtonUp">
                        <Label Content="Paused" Panel.ZIndex="2" Visibility="{Binding Core.Running, Converter={StaticResource invisibilityConverter}, Mode=OneWay}" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="36"/>
                        <Rectangle Visibility="{Binding Core.Running, Converter={StaticResource invisibilityConverter}, Mode=OneWay}" Fill="#7F000000" Stroke="#7F000000" Width="768" Height="576" VerticalAlignment="Top" HorizontalAlignment="Left" Panel.ZIndex="1"/>
                        <Image Width="768" Height="576" HorizontalAlignment="Left" VerticalAlignment="Top" Focusable="True" Source="{Binding Display.Bitmap}"  RenderOptions.BitmapScalingMode="NearestNeighbor" Stretch="Fill"/>
                    </Grid>
                </DataTemplate>
            </TabControl.ContentTemplate>
            <TabControl.ItemsSource>
                <CompositeCollection>
                    <TabItem Header="Home">
                        <TabItem.HeaderTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="Home"/>
                                </StackPanel>
                            </DataTemplate>
                        </TabItem.HeaderTemplate>
                        <TabItem.ContentTemplate>
                            <DataTemplate>
                                <ScrollViewer>
                                    <Grid>
                                        <StackPanel>
                                            <Label Content="Machines" FontWeight="Bold" FontSize="14" />
                                            <ItemsControl ItemsSource="{Binding Source={StaticResource Machines}}">
                                                <ItemsControl.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <WrapPanel/>
                                                    </ItemsPanelTemplate>
                                                </ItemsControl.ItemsPanel>
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Grid MouseLeftButtonUp="MachinePreviewGrid_MouseLeftButtonUp">
                                                            <Grid.ContextMenu>
                                                                <ContextMenu>
                                                                    <MenuItem x:Name="_pausePreviewMenuItem" Header="Pause" IsEnabled="{Binding Core.Running, Mode=OneWay}" Click="PausePreviewMenuItem_Click"/>
                                                                    <MenuItem x:Name="_resumePreviewMenuItem" Header="Resume" IsEnabled="{Binding Core.Running, Converter={StaticResource booleanInverterConverter}, Mode=OneWay}" Click="ResumePreviewMenuItem_Click"/>
                                                                    <MenuItem x:Name="_openPreviewMenuItem" Header="Open" Click="OpenPreviewMenuItem_Click"/>
                                                                    <MenuItem x:Name="_closePreviewMenuItem" Header="Close" Click="ClosePreviewMenuItem_Click"/>
                                                                    <MenuItem x:Name="_removeOpenMachineMenuItem" Header="Remove" Click="RemoveOpenMachineMenuItem_Click"/>
                                                                </ContextMenu>
                                                            </Grid.ContextMenu>
                                                            <StackPanel Margin="5">
                                                                <Image Width="192" Height="144" HorizontalAlignment="Center" VerticalAlignment="Top" Focusable="True" RenderOptions.BitmapScalingMode="Linear" Stretch="Fill" Margin="0" Source="{Binding Display.Bitmap}" ToolTip="{Binding Filepath}" Cursor="Hand"/>
                                                                <Label Content="{Binding Name}" HorizontalAlignment="Center"/>
                                                            </StackPanel>
                                                        </Grid>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                            <Separator />
                                        </StackPanel>
                                    </Grid>
                                </ScrollViewer>
                            </DataTemplate>
                        </TabItem.ContentTemplate>
                    </TabItem>
                    <CollectionContainer Collection="{Binding Source={StaticResource OpenMachines}}" />
                </CompositeCollection>
            </TabControl.ItemsSource>
        </TabControl>
        <StatusBar x:Name="_statusBar" Height="30" Grid.Row="3" VerticalAlignment="Top">
            <TextBlock x:Name="_clockTextBlock" TextWrapping="Wrap" Text="{Binding ActiveMachine.Core.Ticks, Converter={StaticResource ticksConverter}, Mode=OneWay, StringFormat=\{0:h\\:mm\\:ss\}}"/>
            <Separator/>
            <Slider x:Name="_volumeSlider" Width="60" VerticalAlignment="Center" Maximum="255" TickPlacement="BottomRight" TickFrequency="64" Value="{Binding ActiveMachine.Core.Volume, Mode=TwoWay}" ToolTip="{Binding ActiveMachine.Core.Volume}" />
        </StatusBar>
    </Grid>
</Window>
